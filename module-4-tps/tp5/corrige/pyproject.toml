[project]
name = "agent-factory"
version = "0.1.0"
description = "Exemple minimal pour industrialiser un agent Python"
readme = "README.md"
requires-python = ">=3.10"
authors = [{name = "mi-doca"}]

[project.scripts]
agent-factory = "agent_factory.cli:main"
